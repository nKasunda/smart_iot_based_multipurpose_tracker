CORE OR ANY BACKEND FUNCTIONS
1. Receive location data
    - The arduino gadget or the tracker should send gps location(lat,long)
    - the tracker sending its battery percentage
    - strength of connection i do not know if this should be done by the tracker
2. Store tracking data
   - Trackers ID whether assigned to a client/user or not if so the client/user should be determined
   - Location(lat,lng, timestamp)
   - status(online/offline)
3. Serve data to frontend
   - Current location
   - Location history
   - Status
   - warnings or any issue(battery percentage, GMS connectivity status)
4. Realtime updates
   - Live movement on the map
5. Authentication
   - The admin is permitted to track & view tracker location, as well create and assign a client to a tracker
   - Client only view tracker location
